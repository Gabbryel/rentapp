// Deprecated: Prisma setup is no longer used. We keep this file to avoid import errors.
// MongoDB is the active data layer. See lib/mongodb.ts and lib/contracts.ts.
// Export a typed "any" placeholder to prevent runtime import failures.
export const prisma: any = null;
